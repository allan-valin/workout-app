<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Must stay an AppCompat descendant: MainActivity extends AppCompatActivity for per-app locales. -->
    <style name="Theme.WorkoutApp" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
